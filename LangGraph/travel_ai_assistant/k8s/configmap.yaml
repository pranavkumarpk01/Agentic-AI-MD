apiVersion: v1
kind: ConfigMap
metadata:
  name: travel-ai-config
data:
  # Colima users: replace with Colima's gateway IP (run: colima ssh -- ip route | grep default | awk '{print $3}')
  # Docker Desktop users: host.docker.internal works directly
  OLLAMA_BASE_URL: "http://192.168.5.2:11434"
  OLLAMA_MODEL: "llama3.2:3b"
